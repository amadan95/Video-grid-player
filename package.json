{
  "name": "video-grid-streamer",
  "version": "1.0.0",
  "description": "Local video grid player that streams local files via a small server, randomly seeks and rotates every 15s.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mime-types": "^2.1.35"
  }
}
